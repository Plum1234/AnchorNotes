package com.example.anchornotes.model;

/**
 * Enum representing the type of reminder for a note.
 */
public enum ReminderType {
    TIME,       // Time-based reminder
    GEOFENCE,   // Location-based geofence reminder
    NONE        // No reminder set
}
